services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: readerdna
      POSTGRES_PASSWORD: readerdna_dev_pass
      POSTGRES_DB: readerdna
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: